# Search & Recommendation 问题排查方法论

## ① 任意指标异常的第一反应（先稳住）

我不会先假设是模型问题，而是先判断系统和策略是否异常。

---

## ② Search 相关问题的固定拆解顺序

```
Query → Recall → Ranking → Fallback / Latency
```

**铁律一句话：ranking 解决不了 recall 的缺失。**

---

## ③ 「结果不相关」你怎么查

我会先分层定位是 Query、Recall 还是 Ranking。

- 先用可解释信号判断 recall 覆盖是否正常
- 如果候选本身不对，我不会继续看 ranking
- 在确认 recall 正常后，才排查 ranking 的特征、score 分布和 baseline 对照

---

## ④ Query 分桶

常用 **2 个信号**（够用、好解释）：

- **Query 长短** → 意图明确度
- **历史 CTR** → 是否已被很好满足

**面试话术：**

我会用 query 长短和历史 CTR 做分桶，对短且高 CTR 的 head query 用稳定策略，对长或低 CTR 的探索型 query 再交给模型重点处理。

---

## ⑤ Ranking Debug 的 3 个检查点（不要说模型）

### 1. Score 分布
- 是否过于集中 / 被拉平

### 2. Feature 行为
- 是否缺失 / 默认值被大量触发

### 3. Baseline 对照
- popularity / rule 是否反而更合理

**加分句：baseline 是 sanity check，不是为了赢模型。**

---

## ⑥ CTR 暴跌你怎么查（顺序一定别错）

### Step 1：排除"假暴跌"
- 口径 / 日志 / 流量结构

### Step 2：排查系统与策略
- fallback / 降级 / 配置改动

### Step 3：再看 Search
- 分桶 → recall vs ranking

**一句话总结：先怀疑系统，再怀疑策略，最后才怀疑模型。**

---

## ⑦ 修复后，怎么确认不是"假恢复"

- fallback 没有触发
- 分桶后各类 query 都稳定
- 多个时间窗口趋势一致

**高级一点的说法：**

我更关注恢复路径是否符合预期的因果链路，而不仅是指标反弹。

---

## ⑧ GMV 掉了，但 Search 指标正常（高阶必考）

### 先说结论（非常重要）：

**GMV 是业务结果指标，不完全由 Search 决定。**

### 证明「不是 Search」的 3 类证据

#### 1. 订单侧
- CTR 稳定，但 CVR / 客单价 ↓

#### 2. 商品侧
- 高价商品缺货 / 占比下降

#### 3. 用户 / 策略侧
- 新用户占比变化、活动结束

**Search 负责相关性，不对 GMV 单独负责。**

---

# 老板说 GMV 掉了，你只有 1 天时间，先做哪 3 件事？

## 1️⃣ 先做 Query 分桶，找"GMV 掉在谁身上"

**目的：** 先别全站瞎查，先定位是哪些 query 在贡献 GMV 下跌。

### 操作步骤

- 把 query 按 **短/长、head/tail、品牌/品类/描述型** 分桶
- 每个桶看：
  - GMV、订单量、客单价（AOV）、CVR
  - 再对照 CTR（确认不是点击崩了）

**你可以对老板说：**

我先找出 GMV 下跌最集中的 query 类型，这样后面排查 Recall/Rank 才有方向。

---

## 2️⃣ 对"掉得最狠的桶"做 Recall 验证：点击目标还在不在候选里

**目的：** 先判断是不是候选集合变差导致用户买不到。

### 操作步骤

- 选 Top N "GMV 下跌最大"的 query
- 对每个 query：
  - 看昨天 vs 前一周：用户最终购买/加购的商品是否还出现在 recall top-K
  - 如果不在：基本可以判定是 recall / 供给（下架、缺货、过滤）相关问题

**关键话术：**

如果购买目标都不在候选里，Rank 再强也救不了，这是 Recall/供给问题优先处理。

---

## 3️⃣ Recall OK 才查 Rank：看高 GMV 商品是否被"压下去"

**目的：** 候选里有好货但卖不动，通常是排序目标/权重/规则出了问题。

### 你重点看 3 个信号（都是"可解释"的）

1. **Score 分布：** 是否被打平/某特征一票否决
2. **高价/高转化商品的曝光位置：** 是否系统性下滑
3. **Baseline 对照：** popularity / rule 排序是否比模型更像样

**你可以对老板说：**

如果 recall 覆盖没问题，但高 GMV 商品位置整体下滑，那是 Rank/策略问题，我会优先回滚或加 guardrail 止血。
