# 搜推系统各层延迟与降级策略

## 0. 一句话结论
* **最怕慢的层**：在线链路里"会卡住整个请求"的层，尤其是 Ranking / LLM / 跨网络依赖（一慢全慢）。
* **最能降级的层**：Recall / Feature / Rerank / 展示策略（能用更便宜的替代方案"先出结果再说"）。

---

## 1. 哪一层最怕慢（按"拖垮整条链路"的程度排序）

### A. Ranking（精排 / 多特征打分）——"最怕慢的常见第一名"

**为什么怕慢**
* 排序是"最后一公里"，通常**必须等它结束才能返回 TopK**。
* 还经常依赖**特征服务、向量服务、用户画像、实时统计**等多个外部 RPC，一旦链路变长就爆炸。

**典型慢点**
* 特征拉取（多次 RPC、依赖缓存命中）
* 批量打分（大候选集、模型较重）
* Join 特征（线上拼接、数据缺失回退）

---

### B. LLM / Query Understanding（如果放在线上）——"慢 + 不稳定 + 成本高"

**为什么怕慢**
* LLM 往往是**最高延迟方差**：偶发抖动、超时、排队。
* 一旦你"强依赖 LLM 输出才能召回/排序"，就会把整体 SLA 绑死。

**典型慢点**
* 模型推理耗时（尤其外部 API）
* prompt 过长、上下文过大
* retry / fallback 逻辑写得不好导致雪崩

**工业常用姿势**：LLM 更适合做**异步改写/缓存、或离线生成候选/标签**，尽量别成为"主链路强依赖"。

---

### C. 多路 Recall + Merge/Dedup（候选规模上来后）——"不一定最慢，但容易被你搞慢"

**为什么怕慢**
* 多路召回并行没做好就串行。
* 候选数一变大（比如从 2k 到 20k），后面每一层成本都乘上去。

**典型慢点**
* ANN 检索参数过重（HNSW efSearch、IVF nprobe）
* 多路召回串行
* 去重/过滤写得低效（大列表、复杂规则）

---

### D. 特征服务 / 画像服务（外部依赖）——"不一定自己慢，但最怕它慢"

**为什么怕慢**
* 你无法完全控制它：网络、下游负载、缓存穿透都能让你超时。
* 这是线上"抖动"的核心来源之一。

---

## 2. 哪一层能降级（从"最容易做可用降级"到"相对难"）

### A. Recall 层：最适合降级（因为"有候选就能工作"）

**可降级方式（从强到弱）**：
1. 缩小候选规模：topK 从 5000 → 1000
2. 关掉部分召回路：只保留最稳的 1~2 路（如热门/embedding 主路）
3. ANN 参数降档：降低 nprobe / efSearch
4. 直接用缓存候选：上一次请求/同类 query 热缓存

**降级原则**
* 先保证"有结果"，再追求"更相关"。

---

### B. Feature 层：用"缺省值 + 缓存 + 部分特征关闭"

**可降级方式**：
* 只取核心特征（少量强特征），长尾特征跳过
* 允许缺失特征：缺失就用 default，不要阻塞等待
* 缓存画像/统计特征：接受轻微不新鲜

---

### C. Ranking 层：可以降级，但要"有 plan B"

**可降级方式**：
* 轻量模型替代：DNN → LR/GBDT 小模型；或减少层数/特征数
* 二段变一段：精排关闭，仅用粗排分数输出
* 减少打分候选：只对前 N 个候选打精排

---

### D. Rerank / 多样性 / 规则：非常适合"可关可开"

**可降级方式**：
* 多样性、探索、新商家插入、去同质化这些**全部可以关**，最多损失体验但不至于"无结果"。

---

### E. LLM 相关：只适合"软依赖降级"

**可降级方式**：
* 超时即放弃：不等 LLM，直接走原 query
* 用缓存的改写：同 query 命中就用
* 降级到规则改写：同义词表、拼写纠错、slang map

---

## 3. 实战里的"降级优先级"模板

当 latency 超预算，按这个顺序砍：

1. 砍非必要 rerank/规则（多样性、探索、复杂策略）
2. 缩 recall 候选规模（topK、ANN 参数降档、关掉次要 recall 路）
3. 减少特征拉取（只保留核心特征；缺失用 default）
4. ranking 降档（轻量模型、少打分候选）
5. LLM 变软依赖（超时放弃 / 只用缓存 / 规则替代）

**核心目标**：先保证能返回，再逐步恢复质量。
